# Environment variables for AI Code Forge devcontainer
# Copy this file to .env and fill in your values

# Claude Code OAuth Token (Preserves Claude Max subscription)
# Get this by running 'claude setup-token' on your host machine
# This bypasses the Claude Code setup wizard on devcontainer rebuilds
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: Other Claude Code authentication
# ANTHROPIC_API_KEY=  # Alternative authentication (uses pay-as-you-go billing)

# Optional: GitHub authentication (if not using gh auth login)
# GITHUB_TOKEN=

# Optional: Git configuration (if not set via git config)
# GIT_USER_NAME="Your Name"
# GIT_USER_EMAIL="your.email@example.com"

# Optional: MCP server configuration
# PERPLEXITY_API_KEY=

# Development environment settings
PYTHONIOENCODING=UTF-8
RUNTIME_ENV=devcontainer
CONTAINER_ENV=1